package com.divudi.data;

/**
 * Enum for categorizing types of triggers based on their functional area within the system.
 */
public enum TriggerTypeParent {
    PURCHASE_ORDER_REQUEST,
    INPATIENT_ORDER_REQUEST,
    TRANSFER_REQUEST,
    TRANSFER_ISSUE,
    PURCHASE_ORDER_APPROVAL,
    OPD_BILL_CANCELLATION
}
